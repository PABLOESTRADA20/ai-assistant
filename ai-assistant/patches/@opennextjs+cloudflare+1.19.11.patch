diff --git a/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js b/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
index be3f893..e8caf21 100644
--- a/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
+++ b/node_modules/@opennextjs/cloudflare/dist/cli/build/bundle-server.js
@@ -41,6 +41,9 @@ const optionalDependencies = [
     // `server.edge` is not available in react-dom@18
     "react-dom/server.edge",
 ];
+// These packages use native Node.js addons (`.node`) that esbuild cannot bundle
+// and Cloudflare Workers cannot run. The TTS route handles this gracefully at runtime.
+const serverOnlyDeps = ["kokoro-js", "onnxruntime-node", "@huggingface/transformers"];
 /**
  * Bundle the Open Next server.
  */
@@ -100,7 +103,7 @@ export async function bundleServer(buildOpts, projectOpts) {
             // Apply updater updates, must be the last plugin
             updater.plugin,
         ],
-        external: ["./middleware/handler.mjs"],
+        external: ["./middleware/handler.mjs", ...serverOnlyDeps],
         alias: {
             // When @vercel/og is not used, alias the edge entry to a throwing shim so the
             // dynamic `import("next/dist/compiled/@vercel/og/index.edge.js")` call site
